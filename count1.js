var counter = function(arr){
    return `the total ${arr.length} in this array`
}

var totalArray = function(a , b){
    var c = a+b;
    return c;
}

module.exports = {
    counter:counter,
    totalArray:totalArray
}